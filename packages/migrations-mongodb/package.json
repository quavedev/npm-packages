{
  "name": "@quave/migrations-mongodb",
  "version": "1.0.0",
  "description": "MongoDB adapter for @quave/migrations",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint src --config ../../eslint.config.js",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "keywords": [
    "migrations",
    "mongodb",
    "database"
  ],
  "author": "Quave",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/quavedev/npm-packages.git",
    "directory": "packages/migrations-mongodb"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@quave/migrations": "^2.0.0",
    "mongodb": "^7.2.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
